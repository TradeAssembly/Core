[package]
name = "tradeassembly-plugin-sdk"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Public wire contract and bounded JSONL framing for TradeAssembly plugins."
repository = "https://github.com/TradeAssemblyHQ/TradeAssembly"
keywords = ["tradeassembly", "plugin", "sdk"]
categories = ["api-bindings", "data-structures"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
